apiVersion: extensions/v1beta1 # This is the "Bait" - highly deprecated
kind: Ingress
metadata:
  name: deprecated-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: web-service
          servicePort: 80
