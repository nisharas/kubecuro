apiVersion: networking.k8s.io/v1beta1  # <--- HEALER TRIGGERS ON THIS
kind: Ingress
metadata:
  name: needs-healing
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: my-service
          servicePort: 80 # <--- OLD SYNTAX
